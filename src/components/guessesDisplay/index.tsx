import React from "react";
import { IBoard } from "../../types/board";

interface IProps {
  board: IBoard;
}

const GuessesDisplay = ({ board }: IProps) => {
  return <div>GuessesDisplay</div>;
};

export default GuessesDisplay;
